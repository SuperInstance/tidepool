{
  "name": "tidepool",
  "version": "1.0.0",
  "description": "tidepool v1 — helper-thread memory ocean for hermit (Discord bot). Markdown ocean + quilt-WAL projection + quiet-thread negative ledger. Standalone bench; NOT a PR.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test tests/tidepool.test.mjs"
  }
}
